{% extends 'base.html.twig' %}

{% block title %}About{% endblock %}

{% block body %}
    <h1>About</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ about.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ about.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_about_index') }}">back to list</a>

    <a href="{{ path('app_admin_about_edit', {'id': about.id}) }}">edit</a>

    {{ include('admin_about/_delete_form.html.twig') }}
{% endblock %}
