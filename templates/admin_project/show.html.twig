{% extends 'base.html.twig' %}

{% block title %}Nos Projets{% endblock %}

{% block body %}
    <h1>Projet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Numero</th>
                <td>{{ project.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ project.projectTitle }}</td>
            </tr>
            <tr>
                <th>Details</th>
                <td>{{ project.ProjectDetails }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_project_index') }}">Voir la liste</a>

    <a href="{{ path('app_admin_project_edit', {'id': project.id}) }}">Modifier</a>

    {{ include('admin_project/_delete_form.html.twig') }}
{% endblock %}
