{% extends 'base.html.twig' %}

{% block title %}Goal{% endblock %}

{% block body %}
    <h1>Goal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ goal.id }}</td>
            </tr>
            <tr>
                <th>GoalImage</th>
                <td>{{ goal.goalImage }}</td>
            </tr>
            <tr>
                <th>GoalContent</th>
                <td>{{ goal.goalContent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_goal_index') }}">back to list</a>

    <a href="{{ path('app_admin_goal_edit', {'id': goal.id}) }}">edit</a>

    {{ include('admin_goal/_delete_form.html.twig') }}
{% endblock %}
