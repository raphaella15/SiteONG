{% extends 'base.html.twig' %}

{% block title %}Goal index{% endblock %}

{% block body %}
    <h1>Goal index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>GoalImage</th>
                <th>GoalContent</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for goal in goals %}
            <tr>
                <td>{{ goal.id }}</td>
                <td>{{ goal.goalImage }}</td>
                <td>{{ goal.goalContent }}</td>
                <td>
                    <a href="{{ path('app_admin_goal_show', {'id': goal.id}) }}">show</a>
                    <a href="{{ path('app_admin_goal_edit', {'id': goal.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_goal_new') }}">Create new</a>
{% endblock %}
