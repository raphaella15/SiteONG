<form method="post" action="{{ path('app_admin_activity_delete', {'id': activity.id}) }}" onsubmit="return confirm('Voulez-vouz vraiment supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity.id) }}">
    <button class="btn">Supprimer</button>
</form>
